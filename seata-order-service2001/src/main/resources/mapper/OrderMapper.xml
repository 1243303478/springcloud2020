<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atzy.springcloud.dao.OrderDao">

    <insert id="createOrder" parameterType="com.atzy.springcloud.domain.Order" useGeneratedKeys="true" keyColumn="id" keyProperty="id" >
        insert into t_order(`user_id`,`product_id`,`count`,`money`,`status`)
        values(#{userId},#{productId},#{count},#{money},0);
    </insert>

    <update id="updateOrderStatus">
        update `t_order` set status = #{status}
        where id = #{id}
    </update>
</mapper>